# toolchain / install artifacts (regenerable)
node_modules/
.pnpm-home/
.npm-cache/
.bin/

# runtime / test leftovers
*.log
.dshtest/
.ff-profile/
.syntax-test.png

# editor / local tooling — never commit secrets here (API keys end up in these)
.vscode/

# OS
.DS_Store
Thumbs.db
